#pragma once

#include "CutManager.h"

class LandmarkCutManager : public CutManager {

public:
	LandmarkCutManager() {}


private:

};

/*
I think i need a struct - as we will want to keep track
of each "landmark"
*/